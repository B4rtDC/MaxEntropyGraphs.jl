# Logo assets

This folder contains the Julia script that generates the MaxEntropyGraphs.jl logo and the rendered outputs it produces.

Run `generate_maxentropygraphs_logo.jl` to refresh the SVG and PNG variants. The script also copies the light PNG into `docs/build/assets/logo.png` so the generated documentation can pick it up.